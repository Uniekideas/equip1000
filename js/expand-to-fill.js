function expandToFillScreen() {
    var container = document.getElementById('announcementsContainer');
    container.style.height = 'auto'; // Set height to auto to allow it to expand based on content
    container.style.paddingBottom = '50px';
}

// function expandNoteToFillScreen() {
//     var container = document.getElementById('notesContainer');
//     container.style.height = 'auto'; // Set height to auto to allow it to expand based on content
// }